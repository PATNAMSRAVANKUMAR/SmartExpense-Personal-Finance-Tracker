# Vite Frontend Environment Variables
# In development, Vite automatically proxies /api to http://localhost:5000
# In production or standalone, point to the Express backend API URL:
VITE_API_URL=/api
